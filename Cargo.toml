[workspace]
resolver = "3"
members = [
    "proc-core",
    "proc-bin",
    "proc-web",
]

# # Shared settings
[workspace.package]
version = "0.1.0"
authors = []
license = ""
repository = ""

# # Shared dependencies
# [workspace.dependencies]
# dep-name = { version = "1.0", features = ["derive"] }

# # Shared development dependencies
# [workspace.dev-dependencies]
# dep-name = "0.5"

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "s"
